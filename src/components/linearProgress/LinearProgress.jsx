import React from 'react'
import {LinearProgress as TopProgressBar} from '@mui/material';

const LinearProgress = () => {
  return (
    <TopProgressBar color="otherColor"/>
  );
}

export default LinearProgress